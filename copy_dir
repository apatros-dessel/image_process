# Unfinished code to copy a directory for copying Landsat or Sentinel scene

path = r'C:\sadkov\playground\gamma'
l = len(path)
endpath = r'C:\sadkov'
for root, dirs, files in os.walk(path):
    if root != path:
        sroot = root[l + 1:]
    else:
        sroot = ''
    lendpath = '{}\\{}'.format(endpath, sroot)
    if not os.path.exists(lendpath):
        raise Exception
    else:
        os.chdir(lendpath)
    for _dir in dirs:
        os.makedir(_dir)
    for _file in files:

